/**
 *  * Exercicio
 *
 * ? 1.Declare uma variavel contendo uma string
 * ? 2.Declare uma variavel contendo um numero dentro de string
 * ? 3.Declare uma variavel com sua idade.
 * ? 4.Declare duas variaveis, uma com seu nome e outra com sobrenome e some
 * ? 5.Coloque a seguinte frase em uma variavel "It's time"
 * ? 6.Veifique o tipo de variavel que contém seu nome
 *
 */

//* 1.
var nome = "Lucas";

//* 2.
var nomeAltura = "Altura " + 1.78;

//* 3.
var idade = 30;

//* 4.
var nome = "Lucas";
var sobrenome = "Tenório";
var nomeCompleto = nome + " " + sobrenome;

//* 5.
var frase = "Hora de codar: It's Time";

//* 6.
console.log(typeof nome);
